export type QBoxLocationFormFormValues = {
  shortId: string;
  city: string;
  district: string;
  street: string;
  postalCode: string;
  buildingNumber: string;
  secondaryNumber: string;
  installationLocation: string;
  accessInstruction: string;
  qboxImage: string; // Add this
};
