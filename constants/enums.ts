export enum QR_VALIDITY_DURATION_TYPE {
  MIN = "min",
  HOUR = "hour",
  DAY = "day",
}

export enum AUTH_PROVIDERS {
  EMAIL = "email",
  PHONE = "phone",
}
export enum PACKAGE_TYPE {
  INCOMING = "incoming",
  OUTGOING = "outgoing",
  DELIVERED = "delivered",
}
