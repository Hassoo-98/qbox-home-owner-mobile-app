import { Text } from "@/components";
import { View } from "react-native";

export const QRCodeHistory = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>QR Code History</Text>
    </View>
  );
};

export default QRCodeHistory;
